# Labs prototype-only dependencies
# These dependencies are installed IN ADDITION to base.txt and production.txt
# Shared dependencies (python-dateutil, pytz, six, tzdata) come from base.txt
# This file only adds the unique labs dependencies

numpy==2.3.4
    # via pandas
pandas==2.3.3
    # via -r requirements/labs.in
